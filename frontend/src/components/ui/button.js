const Button = () => {

    return (
        <button
            className="btn"
            style={{ color: "white", background: "black" }}
        >
            Virtual Tour
        </button>
    );
};

export default Button;
